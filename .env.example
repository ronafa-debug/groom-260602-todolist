# Frontend (Vite) — 프로덕션에서 API 서버 URL (개발 시 비워두면 /api 프록시 사용)
VITE_API_URL=

# Backend (server/)
JWT_SECRET=change-this-to-a-long-random-string
PORT=3001
# DB_PATH=./server/data.db
# CORS_ORIGIN=https://your-app.vercel.app,http://localhost:5173
